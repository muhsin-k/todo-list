// action types
export const types = {

};


// initial state
export const initialState = {

};


// reducer
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};


// action creators
export const actions = {

};